#include <bits/stdc++.h>
using namespace std;

int main(){
  int n, sum = 0;
  cin >> n;
  char a;

  for(int i = 0, j = 0; i < n; i++){
    cin >> a;
    if(a == '('){
      if()
      } else{

      }
    }

  cout << sum;
}
