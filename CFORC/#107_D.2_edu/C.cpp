#include <bits/stdc++.h>
using namespace std;
#define fastio ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)

int main(){
  fastio;
  int n, m; cin >> n >> m;
  vector<int> pos(51);

  for(int i = 1, x; i <= n; i++){
    cin >> x;
    pos[x] = i;
  }

  for(int i, counter = 0; m--;){
    cin >> i;

    
  }
}