#include <bits/stdc++.h>
using namespace std;
#define fastio ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

int n, x1, y1, x2, y2;
vector<bool> promienie;

int main(){
  int t;
  cin >> t;

  while(t--){
    cin >> n;
    promienie.resize(n+1);

    for(int i = 1; i <= n; i++){
      cin >> promienie[i]
    }
  }
}