#include <bits/stdc++.h>
using namespace std;

int main(){ //ios_base::sync_with_stdio(0);
  int t;
  cin >> t;

  while(t--){
    int n;
    cin >> n;
    int P[n], sum = 0;
    for(int i = 0; i < n; i++){
      cin >> P[i];
    }
    for(int i = 0; i < n; i++){

    }
    cout << sum << '\n';
  }
}
