#include <bits/stdc++.h>
using namespace std;

struct informacja{
  int st, en;
};

int main(){ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
  int n, k, start, ile, odstep;
  cin >> n >> k;
  
  vector<informacja> V(n);

  while(k--){
    cin >> start >> ile >> odstep;
    V[]
    
  }

  for(informacja x : V){
    cout << x << ' ';
  }
}