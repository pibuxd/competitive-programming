#include "bits/stdc++.h"
using namespace std;

int main(){ //ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
  int n; cin >> n;
  char C[n+1];
  for(int i = 1; i <= n; i++){
    cin >> C[i];
  }

  for(int pocz = 1, kon = n; i < )
}
