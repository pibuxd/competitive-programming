#include <bits/stdc++.h>
using namespace std;

int main(){ ios_base::sync_with_stdio(0);
  int n, k; cin >> n; string s; char C[20000];
  for(int w = 0; w < n; w++){ cin >> k >> s;
    for(int i = 0; i < s.size(); i++){

    }
  }
}
