#include <bits/stdc++.h>
using namespace std;

int main(){
  ios_base::sync_with_stdio(0);
  int n, p, suma; cin >> n; vector<long long> S
  for(int i = 0; i < n; i++){
    cin >> p; S.push_back(p);
  }
  for(int i = 0; i < n; i++){
  S[S[i]]
  }
  cout << n;
}
