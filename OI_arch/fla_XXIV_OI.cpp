#include <bits/stdc++.h>
using namespace std;

int main(){ ios_base::sync_with_stdio(0);
  int n, k; cin >> n >> k; long long s = 0;
  int X[n], A[n], B[n], x = 0, y = 0;
  for(int i = 0; i < n; i++){ cin >> X[i] >> A[i] >> B[i];}
}
